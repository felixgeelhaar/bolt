scan:
  exclude:
    - "vendor/"
    - "benchmarks/"
    - "examples/"
    - ".relicta/"
    - ".cover/"
    - "findings.json"

  rules:
    disable: []

    severity_override: {}

output:
  format: json
  directory: .

policy:
  fail_on: high
  warn_on: medium
